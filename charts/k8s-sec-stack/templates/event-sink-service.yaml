apiVersion: v1
kind: Service
metadata:
  name: mcp-event-sink
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: mcp-event-sink
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: {{ .Values.eventSink.nodePort }}
